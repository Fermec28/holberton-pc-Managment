<nav class="navbar navbar-static-top bg-dark navbar-dark">
  <div class="container">
    <div class="row">
      <div class="col-sm-10 col-sm-offset-1">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <%= link_to "Computer Managment", root_path, class: "navbar-brand" %>

        </div>


        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">
            <% if current_user%>
            <li class="navbar nav-item">
              <a href="#" class="name_user"><strong>User:
                  <%=current_user.name%></strong></a>
            </li>
            <%end%>

            <% if (current_user && can?(:read,User)) %>
            <li class="navbar nav-item">
              <%= link_to "Users", admin_path %>

            </li>
            <%end%>
            <% if (current_user && can?(:read,Computer)) %>
            <li class="navbar nav-item">
              <%= link_to "Computers", computers_path %>
            </li>
            <%end%>
            <% if current_user%>
            <li class="navbar nav-item">
              <%= link_to "Log out", destroy_user_session_path, method: :delete %>
            </li>

            <% else %>
            <li class="navbar navbar-item">
              <%= link_to "Sign up", new_user_registration_path %>
            </li>
            <li class="navbar navbar-item">
              <%= link_to "Sign in", new_user_session_path %>
            </li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div>
    </div>
  </div><!-- /.container -->
</nav>